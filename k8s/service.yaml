apiVersion: v1
kind: Service
metadata:
  name: api-gateway-node
  labels:
    app: api-gateway-node
spec:
  type: NodePort
  selector:
    app: api-gateway-node
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
